import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';
import 'bootstrap/dist/css/bootstrap.min.css';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// If you want to start measuring performance in your app, pass a function
// to log results (for example: reportWebVitals(console.log))
// or send to an analytics endpoint. Learn more: https://bit.ly/CRA-vitals

// Booking. Com
// Hotel landing page On react

// Index page (hero or model, logo on left, links on centre, sign in on right, book button which redirects to booking bar, room types, gallery section, facility section, review section, about us, FAQs, footer)
// Room page(types, price, facilities, differences)
// Book now page(landing page, grid or crosal for room look, near by location, price, check out button)
// Checkout page
// Contact page or pop up

reportWebVitals();
